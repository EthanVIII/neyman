

mod tests {
    use neyman::parse::*;

    #[test]
    fn parse_symbols() {
        let x: &str = "\
        if then return test_var
        \
        ";
        let output: Vec<Token> = 
        assert_eq!(1,1);
    }
    
    
    
}